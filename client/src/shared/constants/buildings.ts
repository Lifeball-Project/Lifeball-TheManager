import { BuildingType } from '@/shared/types/buildings';

export const BUILDING_ROUTES: Record<BuildingType, string> = {
  stadium: '/map/stadium',
  home: '/map/house',
  hospital: '/map/hospital',
  store: '/map/store',
};